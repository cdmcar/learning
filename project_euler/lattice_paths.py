
#Starting in the top left corner of a 2×2 grid,
#and only being able to move to the right and down,
#there are exactly 6 routes to the bottom right corner.

#How many such routes are there through a 20×20 grid?

class GridBot:
    if GridBot.__init__(self):

        maxX, maxY = 20
        self.xr = range(x-1)
        self.yr = range(y-1)
        self.path = []
        trails = []

    def map(self, x, y, move):
        self.x = x
        self.y = y
        self.rngX = range(19)
        self.turn = len(move)
        self.move = move
        if self.move == False:
            self.move = 0

        for i in self.rngx:
            while self.x, self.y < 19,19
                for x in range(-x,0) if x != 0:
                    move.append(x)
                for i in range(-19,0):
                    if i < trail[x]:
                        move.append(i)
                for i in range(-19,0):
                    if i < trail[0]:
                        move.append(19)
